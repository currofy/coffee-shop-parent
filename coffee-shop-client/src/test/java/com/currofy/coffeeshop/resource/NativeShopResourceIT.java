package com.currofy.coffeeshop.resource;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeShopResourceIT extends ShopResourceTest {

    // Execute the same tests but in native mode.
}